# Packaged font slot

Card Studio currently uses the configured Windows system fallback (`Arial`). A future licensed pixel-art font belongs in this directory and must be referenced by `config/card_layout.json#font_asset`.

No font is downloaded or bundled yet. Exact cross-machine raster identity remains conditional on the same installed fallback font.
